package munge

import org.specs2.mutable._

class MungerSpec extends Specification { 

  "Munger" should { 
    "read weather.dat" in { 
      val munger = new Munger
//      "weather.dat" must have size(11)
//      munger is not (null)
"This is hopefully true"         ! (1 != 2)
    }
  }
}
